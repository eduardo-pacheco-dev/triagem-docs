# Local test database (SQLite for unit/integration tests)
TEST_DATABASE_URL="file:./prisma/test.db"

# For PostgreSQL (alternative - requires Docker or local PostgreSQL):
# docker compose -f docker-compose.yml up -d
# TEST_DATABASE_URL="postgresql://test:test@localhost:5433/triagem_test"
# TEST_DIRECT_URL="postgresql://test:test@localhost:5433/triagem_test"
