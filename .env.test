# Testing: SQLite (no server needed)
TEST_DATABASE_URL="file:./prisma/test.db"

# Development: MySQL (requires docker compose up -d)
# MYSQL_DATABASE_URL="mysql://root:admin@localhost:3306/triagem_dev"
