# json-server (mock backend — roda com: npm run mock)
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Credenciais do mock (coluna username + password_hash no db.json)
# Usuário padrão: admin / admin123