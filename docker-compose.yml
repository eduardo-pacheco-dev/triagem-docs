version: "3.9"

services:
  postgres-test:
    image: postgres:16-alpine
    container_name: triagem-test-db
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: triagem_test
    ports:
      - "5433:5432"
    tmpfs: /var/lib/postgresql/data
